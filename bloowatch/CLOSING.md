# סגירת יום שוקוגי — נוהל מלא

הקובץ הזה הוא ההוראות של הריצה היומית (19:30 שעון פנמה). ה-Routine עצמו מחזיק
רק את פרטי ההתחברות ומפנה לכאן, כדי שכל שינוי בנוהל ייעשה כאן ולא ידרוש להדפיס
שוב את הסיסמה.

**הקוד:** repo `moshiko3-lab/-`, ענף `claude/new-session-d0r3xc`, תחת `bloowatch/`.

---

## שלב 1 — אילו תאריכים לעבד

**1א.** `mcp__Google_Drive__search_files` עם `parentId = '1qSVbAx_Ho7m6-yy2Tx34OZqfatci4ew6'`.
מצא את `SHOKOGI - DAILY CLOSINGS` → `read_file_content` → חלץ את עמודת `Date`.

**כלל בטיחות: אם קריאת הסיכום נכשלה — עצור מיד ואל תכתוב כלום.** עדיף לפספס יום
מלהרוס את הקובץ.

**1ב.** קבע את רשימת התאריכים:
- **שלושת הימים האחרונים** (היום לפי שעון פנמה, אתמול, ושלשום) — תמיד, גם אם כבר בקובץ.
- **בנוסף:** כל תאריך חסר בין התאריך האחרון שבקובץ לבין היום.
- אל תרד יותר מ-14 יום אחורה בריצה אחת.

הריצה ב-19:30 תופסת את היום לפני שהוא נגמר. הרענון למחרת מתקן את השורה למספר
הסופי. **זו החלפה של אותה שורה, לא כפילות.**

---

## שלב 2 — משיכה

```
python3 bloowatch/daily_report.py --json YYYY-MM-DD [...]
```

מחזיר שורה לכל תאריך עם העמודות המדויקות. הסקריפט עושה את כל האימותים בעצמו.

אם הוא נכשל — דווח את השגיאה כמו שהיא. **לעולם אל תמלא מספרים משוערים ואל תיפול
חזרה לקבצי CSV ידניים.**

---

## שלב 3 — עדכון קובץ הסיכום (פעם אחת, בסוף)

עמודות — בדיוק אלה, בלי להוסיף ובלי להוריד:

```
Date,Day,Transactions,Total,Credit,Cash,Web,OtherPay,Packages,Lessons,Rentals,Photography,Other,Refunds,Check,Notes
```

1. קח את הטבלה הקיימת והמר ל-CSV
2. תאריך שכבר קיים — **החלף את השורה**. חדש — הוסף.
3. מיין לפי תאריך עולה
4. **ודא: אין תאריך כפול.** אם יש — עצור ודווח, אל תכתוב.
5. `create_file`: title=`SHOKOGI - DAILY CLOSINGS`,
   parentId=`1qSVbAx_Ho7m6-yy2Tx34OZqfatci4ew6`, contentMimeType=`text/csv`
6. `trash_file` על הישן
7. שמור את ה-`viewUrl` החדש למייל

---

## שלב 4 — מייל אחד

`mcp__Gmail__send_message` אל `moshiko3@gmail.com`, נושא `סגירת יום שוקוגי — DD/MM/YYYY`.

`htmlBody` עטוף ב-`<div dir="rtl">`:

- **בראש המייל, בולט: בלוק סגירת הקופה** — ראה למטה. זה הדבר הראשון שנקרא.
- **היום:** מספר עסקאות, סה"כ, ופילוח קטגוריות: השכרות / שיעורים / חבילות / צילום.
  **בדולרים שלמים**, בלי אגורות. הם מסתכמים בדיוק לסך היום. קטגוריה שיצאה 0 —
  אל תציג לה שורה.
- **שורת "עודכן ברענון" — רק אם `Total` השתנה** (או `Transactions` / `Refunds`).
  לדוגמה: "עודכן: 27/08 מ-320 ל-355". **אם הסכום לא השתנה — אל תכתוב כלום.**
  בפרט: אל תדווח על שינוי בחלוקת הקטגוריות. החלוקה של Bloowatch זזה בשברי סנט
  בין קריאה לקריאה גם ביום סגור, וזה לא שינוי — זה רעש. דיווח עליו גורם ליום
  גמור להיראות כאילו הוא זז.
- כפתור קישור לקובץ.
- `Check == "CHECK!"` → באנר אדום בראש עם התוכן של `Notes`.
- `Refunds` > 0 → ציין במפורש.

שלח גם `body` טקסט פשוט כגיבוי. **מייל אחד בלבד.**

### בלוק סגירת הקופה (web / credit lesson / cash lesson)

בדיוק שלוש שורות, בסדר הזה, ליום האחרון בלבד:

| | |
|---|---|
| web | `Web` |
| credit lesson | `Credit` |
| cash lesson | `Cash` |
| **total** | `Total` |

כל אמצעי תשלום חריג (Check, Money transfer, OTA Payment) מקבל שורה משלו אחרי
השלוש, עם השם שלו. `OtherPay` הוא הסכום שלהם ביחד.

**כללי ברזל לבלוק הזה:**

1. **מספרים שלמים בלבד, בלי אגורות.** כל מחיר שבית הספר גובה הוא מספר שלם, ולכן
   גם הסכום היומי לכל אמצעי תשלום שלם: 513, 25, 15 — לא 512.87. אם משהו יוצא עם
   אגורות, הסקריפט כבר רשם את זה ב-`Notes` וסימן `CHECK!`. **הצג את מה שהתקבל
   וציין את האזהרה. אל תעגל בעצמך כדי ש"ייראה נכון".**
2. **אין חנות.** יובל רושם גם `credit shop` ו-`cash shop` — מכירות החנות. הן
   **לא נמצאות** בדוח הזה, הן מגיעות ממערכת אחרת, והן לא מספרים עגולים. אל תמציא
   אותן, אל תגזור אותן מההפרש, ואל תוסיף להן שורה ריקה.
3. **אל תשתמש בפילוח הצולב של Bloowatch כמספרי קופה.** בדוח יש בלוקים
   `Account = <אמצעי תשלום>` שמפרקים כל אמצעי לשיעורים/השכרות — הם מגיעים עם
   ארבע ספרות אחרי הנקודה, כי הזמנה מעורבת מפוצלת בין קטגוריות לפי יחס. אלה
   חלקים של תשלום, לא כסף שמישהו לקח. הסקריפט קורא אותם ומאמת מולם את סכומי
   אמצעי התשלום, וזה כל תפקידם. **הם לא נכנסים למייל.**

מאיפה המספרים באמת מגיעים: כל תשלום בודד ביום, מסוכם לפי איך ששולם. זה בדיוק מה
שכתוב בבלוק `Methods` של הדוח, וזה מה שהעמודות `Web` / `Credit` / `Cash` מחזיקות.

**אותו כלל חל על פילוח הקטגוריות.** גם הוא מגיע מהדוח עם ארבע ספרות אחרי הנקודה,
מאותה סיבה בדיוק. הסקריפט כבר מעגל אותו לדולרים שלמים כך שהחלקים מסתכמים בדיוק
לסך היום — העמודות `Packages` / `Lessons` / `Rentals` / `Photography` / `Other`
כבר מגיעות שלמות. **הצג אותן כמו שהן ואל תוסיף אגורות בחזרה.**

השמות בעברית לפילוח הקטגוריות: `LESSONS` = שיעורים, `BOARD RENTALS` = השכרות,
`PACKAGES` = חבילות, `PHOTOGRAPHY` = צילום, `VIDEO ANALYSIS` = ניתוח וידאו.
קטגוריה שלא מוכרת — כתוב את השם כמו שהוא, אל תתרגם בניחוש.

---

## לסיום

דווח: אילו תאריכים נמשכו, אילו שורות נוספו ואילו עודכנו, האם האימות עבר, ולאן
נשלח המייל. **אל תכלול את הסיסמה בדיווח.**
