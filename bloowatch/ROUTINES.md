# השגרות היומיות של שוקוגי — הנוהל

הקובץ הזה הוא מקור האמת לשגרות האוטומטיות. כל שגרה מחזיקה רק הפניה לפרק
שלה כאן, בדיוק כמו `CLOSING.md`, משלוש סיבות:

1. **נוהל שכתוב ב-prompt נקרא כל פעם מחדש על ידי קורא אחר, וסוחף.** כאן הוא
   בגיט: אפשר לראות מה השתנה, מתי, ולמה.
2. **אסימון שכתוב בשש שגרות הוא שישה עותקים של אותו סוד.** ב-2/9/2026 לחיצה
   אחת על refresh בקונסולת Green-API הרגה את הערך שהיה כתוב בחמישה מקומות.
   כאן אין אסימונים בכלל — הם מגיעים ממשתני הסביבה.
3. **prompt קצר הוא prompt שאפשר לשנות בלי לשבור.** לערוך 5000 תווים של נוהל
   בתוך שדה טקסט של שגרה זה איך שהנוסח סוטה מהקוד.

---

## כלל שחוזר על כל השגרות

**שגרה לא מסתיימת לפני שהמשלוח יצא.** לא "הכנתי את הנתונים", לא "התוכנית
מוכנה" — יצא או לא יצא. אם משהו חוסם את השליחה, זה מדווח מיד ולא נשמר לתור
הבא. **ב-11/9/2026 סגירת היום הוכנה במלואה ב-19:40 והמייל יצא יום אחרי,**
ובאותו יום שמונה משבצות תזכורת הוכנו ולא נשלחו, כי מי שהריץ אותן סיים את
התור אחרי שלב ההכנה. הנתונים היו נכונים. זה לא עזר לאף אחד.

**כל שגרה רצה בסשן משלה.** לא בסשן משותף קבוע. שגרה שקשורה לסשן ארוך נופלת
יחד איתו, וזה בדיוק מה שקרה ב-11/9: הווטשדוג ושגרת חידוש Green-API, שתיהן
בסשן נפרד, עבדו כרגיל באותו יום שבו כל השאר שתקו.

**ההוכחה, ב-14/9/2026: 36 התראות שלא נקראו.** כשהמיגרציה הסתיימה התברר
שבתור של הסשן הקבוע חיכו 36 הודעות הערה משגרות של 12/9 ו-13/9 — כל משבצת
:10 ו-:40 של יום שלם, הלו״ז של 19:00, הלו״ז האישי של 19:15, בדיקת השינויים
של 20:00 והתחזית של 18:00. **השגרות ירו בזמן, כל אחת מהן.** ההערה פשוט
נערמה בתור של סשן עסוק ואף אחת מהן לא רצה. מכאן שני דברים:

1. **שגרה שקשורה לסשן קבוע אינה שגרה** — היא בקשה מנומסת שתטופל אם וכאשר.
   `create_new_session_on_fire` הוא ההבדל בין ריצה לבין תור.
2. **תור ישן הוא מסוכן, לא רק חסר תועלת.** לקרוא היום הערה מלפני יומיים
   ולבצע אותה פירושו לשלוח תזכורת לשיעור שכבר נגמר, או לו״ז של יום שעבר.
   **הערה שהזמן שלה עבר — אל תבצע. דווח שהיא הגיעה באיחור ומחק.**

**הרץ רק את הפקודות שכתובות כאן. אל תאלתר אבחון.** אם פקודה נכשלה, דווח את
השגיאה כמו שהיא ועצור — **אל תתחיל לחקור** את הגדרות הגיט, הרשת או הסביבה.
הפקודות שבנוהל מאושרות מראש; פקודה מאולתרת עוצרת ומחכה לאישור אנושי, ובשעה
שש בערב אין מי שיאשר. **ב-13/9/2026 שגרת התחזית של 18:00 עשתה בדיוק את זה:**
היא רצה ב-18:02, נתקלה בבעיה בגיט, ניסתה `git config --get-regexp '^url\.'`
כדי להבין אותה, ונתקעה שם. התחזית לא יצאה, הסשן נשאר תלוי, והבעלים גילה
ב-19:00 שלא הגיע כלום. **ריצה שנתקעה בהמתנה לאישור גרועה מריצה שנכשלה**, כי
היא לא מדווחת ואפשר להעיר אותה מאוחר יותר ולשלוח פעמיים.

**התראות דלוקות בכל השגרות — אל תכבה אותן.** שגרה ששולחת ונכשלת בשקט היא
בדיוק מה שהסתיר את הכשלים של 11/9 ו-13/9.

## הכנה, זהה בכל שגרה

```
[ -d /home/user/-/.git ] || git clone https://github.com/moshiko3-lab/- /home/user/- ; cd /home/user/- && git fetch origin claude/new-session-d0r3xc && git checkout claude/new-session-d0r3xc && git pull origin claude/new-session-d0r3xc && python3 -m pip install --quiet --disable-pip-version-check --retries 5 --timeout 120 playwright requests && echo READY
```

**השורה מתחילה בשכפול ומסתיימת בהתקנת התלויות. שני החצאים נלמדו בדרך
הקשה, כל אחד ביום אחר, וכל אחד עלה בשליחות שלא יצאו.**

### ואל תשנה בה כלום. גם לא לטובה.

**ב-15/9/2026 בערב שיניתי בה שני דברים שנראו כמו שיפור, ושניהם נחסמו.**
`git pull` הוחלף ב-`git reset --hard` (כדי לשרוד שכתוב היסטוריה), והתקנת
התלויות הועברה לסקריפט ב-repo, `sh bloowatch/routine_setup.sh` (כדי
שרשימת התלויות תהיה במקום אחד ולא בעשרה טקסטים). שתי המטרות נכונות.
**הסנדבוקס חסם את שתי הפקודות**, שגרת הלו״ז של 19:00 נעצרה, והלו״ז נשלח
ידנית ב-19:06.

**מה שנלמד מזה, וזה הכלל החשוב בפרק:** הקונטיינר של שגרה רץ במצב הרשאות
`auto`, והוא **לא** אותו מצב שבו רצה בדיקה שנפתחת מסשן עבודה. בדקתי את
הסקריפט בקונטיינר נקי, הוא עבר, והסקתי שהוא בטוח — הבדיקה רצה במצב
הרשאות אחר. **בדיקה שלא רצה בתנאים של שגרה אמיתית לא מוכיחה כלום על
שגרה.**

לכן: **כל צורת פקודה שלא רצה בשלב 0 כבר — היא סיכון, לא שיפור.** `python3`
ו-`python3 -m pip` בטוחים כי הם רצים כאן כל יום. `sh <script>` לא.

### `--retries 5 --timeout 120`

**אותו ערב, שעה אחר כך, שגרת 20:00 נפלה שוב** — הפעם על
`ReadTimeoutError: HTTPSConnectionPool(host='files.pythonhosted.org')`.
ההורדה מ-PyPI נתקעה. **זו לא חסימה ולא באג — זו רשת**, והיא נכשלת לפעמים.
אותה שורה בדיוק עבדה ב-19:16 וב-19:50 באותו ערב.

הדגלים נוספו בגלל זה. **התיקון העמוק יותר הוא להתקין את התלויות בסביבה
עצמה**, כך שאף שגרה לא תוריד כלום בזמן אמת; עד שזה ייעשה, ההורדה היא
נקודת כשל יומית ואלה הדגלים שמקטינים אותה.

**כשההתקנה נכשלת — השגרה עוצרת לפני השליחה ולא מריצה את שלב 1.** זה מכוון:
ריצה בלי התלויות מתה באמצע השליחה, ועצירה נקייה משאירה את רשת הביטחון
לעשות את שלה.

**ב-14/9/2026 קונטיינר של שגרה עלה ריק.** לא חסר קובץ, לא נפל סקריפט —
`/home/user` היה **ריק**: בלי `bloowatch`, בלי `.git`, בלי שום דבר. כל שגרה
ירתה בזמן, הריצה את `git fetch`, קיבלה `fatal: not a git repository`,
דיווחה ועצרה. **יום שלם, אף הודעה אחת לא יצאה משום שגרה.**

זה קרה בדיוק כשעברנו לשגרות בסשן נקי. המעבר עצמו היה נכון — הוא פתר את
בעיית התור — אבל השגרות נוצרו **בלי מקור גיט**, ומאותו רגע כל קונטיירר
שעלה היה ריק. `create_trigger` לא מקבל `source_url` בכלל, אז הפתרון הוא
שהשגרה משכפלת בעצמה. **ה-repo ציבורי, אז זה לא דורש שום אישור או אסימון.**
אם `.git` כבר קיים, השכפול מדולג.

**ההוכחה המלאה, 15/9 21:47 פנמה — קונטיינר ריק אמיתי.** נפתח סשן חדש
באותה סביבה, בלי מקור גיט, בדיוק בתנאים של שגרה, והוא דיווח שלוש שורות:
**הקונטיינר עלה ריק. השכפול הצליח. preflight ירוק — וואטסאפ `authorized`,
בלו תקין, 18 ימי גאות, יציאה `0`, וכל שישה הסודות מ-`environment`.**

זה סוגר את שתי השאלות שנשארו: הקונטיינר באמת עולה ריק (זה השורש), והשורה
הזאת באמת מתקנת את זה מאפס — לא בהיסק, לא בבדיקה מהמכונה הזאת, אלא בריצה.

**מה שנבדק אחרי התיקון, בריצת שגרה אמיתית (15/9, 21:02 פנמה):** שלב 0
הדפיס `READY`, **כל שישה הסודות הגיעו מ-`environment`**, וואטסאפ
`authorized`, בלו תקין, יציאה `0` בלי אזהרות. **משתני הסביבה כן מגיעים
לקונטיינר של שגרה** — זו הייתה שאלה פתוחה יום שלם, והיא סגורה.

### החצי השני: הקוד הגיע, התלויות לא

**ב-15/9/2026 התחזית של 18:00 ירתה בזמן, שכפלה, הריצה, ומתה על:**

```
ModuleNotFoundError: No module named 'playwright'   (surfline.py:151)
```

היא שלחה כלום. אבל הבדיקה ביומן אחר כך הראתה שזאת לא הייתה תקלה של ערב
אחד. חיפוש של כל שורת `18:00 forecast` ו-`19:00 rota` שאי פעם נרשמה החזיר
רק `vm` (בדיקות) ו-`hand` (ידני). **אף ריצה אוטומטית מעולם לא שלחה תחזית
או לו״ז.** התזכורות עבדו כל הזמן — 19 ריצות אוטומטיות מוצלחות באותו יום —
כי הן העבודה היחידה שלא צריכה דפדפן.

**התיקון של 14/9 היה חצי תיקון.** הוא הכניס את הקוד לקונטיינר. שום דבר לא
הכניס אחריו את התלויות שלו, ושתי העבודות שצריכות דפדפן נכשלו בשקט מאז.

**למה זה היה שקט, ולמה זה החלק החמור:**

- שגרה שנעצרת **לפני** השליחה אין לה `RESULT` לדווח ליומן.
- שתי רשתות הביטחון בודקות את **השער**, והשער דיווח נכון שלא נשלח כלום.
- בדיקת המוכנות של 04:30 דיווחה ירוק — היא קוראת סודות, שואלת את וואטסאפ,
  נכנסת לבלו וסופרת גאות, **ואף אחד מהם לא נוגע בדפדפן.**

שלושתן אמרו אמת ואף אחת לא יכלה להגיד למה. **בדיקת מוכנות שלא מפעילה את
מה שהערב צריך היא לא בדיקת מוכנות** — היא רק קונה שקט. זה תוקן:
`preflight.py` בודק עכשיו את התלויות, וחוסר הוא `FAIL` ולא `WARN`.

**התלויות נמצאות בשורת שלב 0 עצמה — `pip install playwright requests`.**
זה לא מה שרציתי. `bloowatch/routine_setup.sh` נכתב כדי שהרשימה תשב
ב-repo ולא בעשרה טקסטים, **והסנדבוקס חסם את הרצתו** (ראה "ואל תשנה בה
כלום" למעלה). הסקריפט נשאר בקוד ועובד כשמריצים אותו ביד, אבל **שלב 0
מתקין ישירות**, וכשתלות משתנה צריך לערוך את כל השגרות. זה חוב ידוע.

ההתקנה **לא מורידה דפדפן** — כרומיום כבר בתמונה תחת `/opt/pw-browsers`
ו-`shot.chromium()` מוצא אותו שם.

**ההוכחה, 15/9 19:16 פנמה — ריצה אוטומטית אמיתית.** הלו״ז האישי יצא
משגרה, מקונטיינר שעלה ריק: `19:15 personal | auto 01RaKu | planned=7
sent=ok`. זו הפעם הראשונה אי פעם ששגרה אוטומטית השלימה שליחת ערב. ב-19:50
רשת הביטחון עברה גם היא אוטומטית.

**וההוכחה ההפוכה, מאותו ערב, שכדאי לזכור יותר מהראשונה:** שעתיים קודם
נבדק הסקריפט בקונטיינר נקי והוא **עבר** — `SETUP OK`, ו-`would send
surfers_he, surfers_en`. playwright עבד, הים נקרא, הגרף צולם, אפס הודעות
נשלחו. **על סמך הבדיקה הזאת נדחף שלב 0 שהסנדבוקס חסם שעה אחר כך.** הבדיקה
לא שיקרה; היא פשוט רצה במצב הרשאות אחר מזה של שגרה.

**שתי המסקנות שמעבר לתקלה:**

1. **שגרה יכולה להיכשל באופן שאף אחת מרשתות הביטחון לא רואה**, אם היא
   נכשלת **לפני** שהיא נוגעת בערוץ שהן מנטרות. כשמשהו נראה תקין חודש ואף
   פעם לא הוכח בריצה אמיתית — **תבדוק ביומן מי באמת שלח, לא אם דווח
   שנשלח.** השורה `hand` מול `auto` היא ההבדל.
2. **בדיקה מוכיחה רק את מה שרץ בתנאים של הדבר הנבדק.** קונטיינר שנפתח
   מסשן עבודה הוא לא שגרה. אם צריך להוכיח שלב 0 — יורים שגרת בדיקה
   אמיתית (`create_trigger` עם `create_new_session_on_fire`, ואז
   `fire_trigger`), ומוודאים בפלט שהיא רצה ב-`permission_mode: auto`.

**`git push` מסשן של שגרה חסום, וזה לא ייפתר מצד השגרה.** הפרוקסי מחזיר
`moshiko3-lab/- is not in this session's authorized repository set`.
קריאה מה-repo הציבורי עובדת; דחיפה דורשת הרשאה שאין לסשן שנוצר משגרה.
**לכן אף שגרה לא מנסה לדחוף.** הדיווח של השגרה הוא הערוץ — הוא מגיע
לבעלים ב-push ובמייל, והוא מה שפתר את התקלה הזאת.

**איך זה התגלה, ולמה זה לקח יום.** השגרות **דיווחו נכון** — הן אמרו בדיוק
מה קרה, ואפילו נמנעו מלאלתר תיקון כי כך כתוב להן. הדיווח הלך בהתראת push
לבעלים ולא לסשן שתיקן. **כשמשהו לא עובד, הדיווח של השגרה הוא המקור הראשון
לקרוא — לא לנחש מהצד.**

השורש נמצא בדיווח הזה, אחרי יום שלם של ניחושים על סודות, על מבנה הפקודות
ועל הקוד — **כולם היו תקינים.**

**כל שישה הסודות אמורים להגיע מהגדרות הסביבה `Bloowatch`**:
`BLOOWATCH_URL`, `BLOOWATCH_EMAIL`, `BLOOWATCH_PASSWORD`, `GREENAPI_ID`,
`GREENAPI_TOKEN`, `GREENAPI_URL`. **אל תבקש אותם מהבעלים ואל תנחש אותם.**

**אין יותר קובץ גיבוי, ואין אסימון כתוב בשום שגרה.** ב-15/9/2026 הוסר
ה-`printf` שכתב `.env` מכל תשע השגרות ששלחו, אחרי ש**שתי ריצות בקונטיינרים
אמיתיים דיווחו שכל שישה הסודות מגיעים מ-`environment`** — בדיוק התנאי
שנקבע כאן קודם, והפעם הוא התקיים בריצה ולא בהיגיון.

**שתי סיבות, ושתיהן אמיתיות.** הראשונה: הגיבוי היה מיותר, כי הסביבה
מגיעה. השנייה חשובה יותר — **סשן שקורא אסימון חי בתוך ההוראה שלו עלול
לסמן את זה כחשיפת סוד ולעצור.** זה קרה בפועל באותו לילה, בבדיקה שהורצה
בקונטיינר נקי: הסשן סירב להמשיך והמליץ להחליף את האסימון. שגרה שעוצרת כך
ב-05:10 היא תזכורת שלא יצאה.

**אם `send.py` מחזיר `error: no gateway configured`** — משתני הסביבה לא
הגיעו לקונטיינר. **עצור, אל תשלח כלום, ודווח כתקלה דחופה.** אל תנחש
אסימון, אל תכתוב אותו ידנית, ואל תבקש אותו בצ׳אט. **הסיכון בשקט גדול
מהסיכון בדיווח.**

**`BLOOWATCH_*` מעולם לא היו בשום שגרה** — הסיסמה לא נכתבת לשום קובץ ולשום
prompt. אם הם לא מגיעים מהסביבה, הלוח לא נקרא והשגרה עוצרת ומדווחת.

**למה זה לא נשאר כטקסט בתוך השגרות.** `export` לא שורד בין פקודות: כל
פקודה שרצה מקבלת תהליך משלה, ולכן `export GREENAPI_TOKEN=...` בפקודה אחת
כבר לא קיים בפקודה שמריצה את `send.py`. השגרות הישנות שרדו במקרה כי
הסודות והשליחה ישבו באותו בלוק, וברגע שהם הופרדו — **ב-13/9/2026 שמונה
תזכורות לחמישה מדריכים לא יצאו**, וכל ריצה דיווחה שהכול תקין. משתנה סביבה
לא תלוי באיך שהריצה מתחלקת לפקודות.

```
python3 preflight.py
```

**זו הדרך לבדוק**, לא `python3 -c "import os..."`. היא מראה לכל סוד אם הוא
קיים ומאיפה, בודקת שהאסימון עוד תקף מול Green-API, ולא מדפיסה שום ערך.

הסימן לתקלה בזמן ריצה הוא `error: no gateway configured` מ-`send.py` —
ואז **עצור, אל תשלח כלום, ודווח.** מאז שהגיבוי חזר, המשמעות חריפה יותר:
**גם הסביבה וגם `.env` חסרים.** **אל תנחש אסימון, אל תכתוב אותו ידנית,
ואל תבקש אותו בצ׳אט.**

**הכלל שמסיים כל שגרה ששולחת:** אם היו שורות בתוכנית ואף אחת לא נשלחה —
**זו תקלה, לא ריצה שקטה.** דווח אותה בפירוט: כמה היו, מה החזיר `send.py`,
ומה עצר. ריצה שבנתה תוכנית ולא שלחה כלום נראית בדיוק כמו משבצת ריקה, וזה
בדיוק מה שהסתיר את הבוקר של 14/9.

**לעולם אל תדפיס את הסודות ואל תבצע commit שמכיל אותם.** `.env` נכתב לדיסק
בכוונה, אבל `.gitignore` חוסם אותו — **אל תדפיס את תוכנו ואל תוסיף אותו
ל-commit.** ה-repo הזה ציבורי.

לפני כל שליחה:

```
curl -sS "$GREENAPI_URL/waInstance$GREENAPI_ID/getStateInstance/$GREENAPI_TOKEN"
```

חייב `{"stateInstance":"authorized"}`. **`401`** — האסימון הוחלף בקונסולה.
**`notAuthorized`** — המספר התנתק. **`403` שגופו `CONNECT tunnel failed`** —
הפרוקסי חסם, זו מדיניות רשת. בכל שלושת המקרים: **אל תשלח, דווח, אל תעקוף.**

`/tmp/plan.json` ו-`/tmp/crew.json` מחזיקים מספרי טלפון של הצוות. **אל תדפיס
אותם ואל תבצע להם commit.** מחק בסוף כל ריצה.

---

## איפה רואים מה כל שגרה עשתה

```
cd bloowatch && python3 report.py --read
python3 report.py --read --minutes 90
```

כל שגרה מתייקת את שורת ה-`RESULT` שלה ליומן WhatsApp, בצ׳אט של בית הספר
לעצמו. שורה נראית כך:

```
09-15 07:41  reminders | auto 7f2a1c | 09-15 12:41Z | RESULT planned=2 accepted=2 gateway=ok journal=2/2
```

- `auto` — השגרה רצה לבד. `hand` — מישהו הריץ את זה ביד. **זה ההבדל
  שנושא את כל המשמעות** כשקוראים את הלוג למחרת.
- `7f2a1c` — ראש מזהה הקונטיינר, כדי שכל השורות של אותה ריצה יתקבצו.
  שם המכונה חסר תועלת לזה: כל קונטיינר כאן נקרא `vm`.

**התיקוף הוא בקוד, לא בפרומפט.** אף שגרה לא צריכה לומר "דווח" — הסקריפט
מתייק את השורה האחרונה של עצמו. פרומפט נקרא מחדש על ידי מודל אחר בכל
ריצה, והשלב בסופו הוא בדיוק זה שנשמט; כך הלו״ז של 19:00 יצא ארבע פעמים
בלי הצילום שלו. סקריפט לא שוכח לקרוא לשורה האחרונה שלו.

**גם ריצה נקייה מתויקת.** רשת ביטחון ששותקת כשהכל תקין נראית בדיוק כמו
רשת ביטחון שלא רצה בכלל, ולהבדיל ביניהן זו כל הסיבה שהמנגנון קיים.

**דיווח לעולם לא מפיל ריצה.** אין שער, 401, נפילת רשת — הכל נכתב ל-stderr
והיציאה נשארת 0. לו״ז שיצא לא נרשם כשבור כי הפתק עליו לא יצא.

`SHOKOGI_NO_REPORT=1` מכבה את התיוק. כל קבצי הבדיקות מגדירים אותו: הם
מריצים את אותם מסלולים עם האישורים האמיתיים, ובלעדיו כל הרצת בדיקות
הייתה מפזרת הודעות.

---

## 1. תזכורת למדריכים — :10 ו-:40

**שתי שגרות, לא אחת.** 05:10–18:10 ו-05:40–18:40 שעון פנמה, שתיהן מריצות
בדיוק את אותן פקודות. הפער של חצי שעה הוא מה שנותן לכל שיעור **50 דקות
התראה בדיוק**: שיעור בשעה עגולה נתפס ב-:10, שיעור בחצי שעה נתפס ב-:40.
**אל תשנה את הדקה של אחת בלי השנייה.** אם הפער מפסיק להיות 30 דקות, שיעור
יקבל תזכורת פעמיים או שלא יקבל בכלל.

**דקה מפורשת ב-cron נשמרת בדיוק.** אל תחזיר את זה ל-`0 * * * *` — לוח על
דקה אפס מעוגן לרגע היצירה וסוחף, וזה מה ששלח תזכורת ב-07:22 לשיעור של 09:00.

```
python3 remind.py
```

**פקודה אחת, ומ-14/9/2026 זה המבנה ולא נוחות.** עד אז המשבצת הייתה שתי
פקודות — `rota.py --remind --plan` ואז `send.py --batch` — ו**ריצה שנעצרה
בין השתיים נראתה בדיוק כמו משבצת שאין בה למי לשלוח**: שתיהן שקטות, שתיהן
מסתיימות נקי, שתיהן נרשמות כ-SUCCEEDED. ב-14/9 ארבע משבצות עשו בדיוק את
זה: שש תזכורות לא יצאו, שום דיווח לא אמר את זה, וזה נמצא רק בקריאה ידנית
של יומן Green-API — אחרי ששניים מהשיעורים כבר התחילו. **אל תפצל את זה
חזרה לשתי פקודות.**

אל תעביר `--from` או `--to` — ברירת המחדל בקוד היא ‎(35, 65]‎, בדיוק ברוחב
הפער בין השגרות. כל הכללים על מי מקבל ובאיזו שפה נשארים ב-`rota.plan`
ובקובץ `whatsapp.json`; `remind.py` לא מחליט כלום מזה בעצמו.

**קוד היציאה הוא הטענה, ואין מצב שלישי:**

- **`0`** — או שלא היה למי לשלוח (`RESULT planned=0`), או שכל מי שהיה אמור
  לקבל קיבל. **סיים בשקט.**
- **`1`** — מישהו היה אמור לקבל ולא קיבל. **זו תקלה שצריך לדווח בפירוט**,
  עם שורת `RESULT` כמו שהיא.

`remind.py` סופר את התוכנית **לפני** השליחה ובודק את יומן Green-API
**אחריה**, ולכן `200` שלא הותיר זכר ביומן נספר ככישלון. לוח שאי אפשר לקרוא
הוא כישלון גם הוא — הוא אף פעם לא "יום בלי שיעורים".

- **אל תשלח שוב מה שנכשל.** תזכורת רלוונטית לשעה הקרובה בלבד, ושליחה
  כפולה גרועה מאיחור.
- **אל תשלח תזכורת שהתאחרה.** אם השיעור כבר התחיל — דווח, אל תשלח.

**רק אם נשלחו הודעות או שהיציאה הייתה `1`** — דיווח. אחרת אל תכתוב כלום.

## 2. תחזית לקבוצות הגולשים — 18:00

**המקור הוא Surfline**, מ-4/9/2026 לבקשת הבעלים. surf-forecast קורא נמוך
בכשליש מטר ומשמש רק כגיבוי. שעות הגלישה 06:00–19:00; ההמלצות נחתכות
ל-07:00–17:00. הקוד עושה את שניהם לבד.

```
python3 evening.py forecast
```

**פקודה אחת, ולא שש.** היא מושכת את הים דרך ה-Chromium שכבר על המכונה,
בונה את ההודעה בשתי השפות מאותם מספרים, מצלמת את הגרף, ושולחת כל קבוצה
פעם אחת. הריצה כולה כ-30 שניות.

`--only he` / `--only en` לקבוצה אחת בלבד — זה מה שרשת הביטחון מריצה כשרק
אחת מהן חסרה. `--dry-run` בונה ומראה לאן היה הולך. `--print` מדפיס את
ההודעות ולא נוגע ברשת. `--no-chart` שולח בלי הגרף.

### הגרף של Surfline

מ-15/9/2026 התחזית נושאת צילום מסך של **גרסת הפלאפון** של Surfline, של
**היום שהתחזית מדברת עליו בלבד**. בפנים: גובה הגלים במילים ובמספר,
מרכיבי הסוול, גלים ורוח לפי שעה, עקומת גאות, וזריחה ושקיעה.

היום נבחר **בלחיצה על בורר הימים בדף**, לא בחיתוך — כך כל הגרפים
מצטיירים מחדש לאותו יום.

**הגרף הוא בונוס וההודעה היא העיקר.** `surfshot.shoot()` לא זורק חריגה
לעולם: הוא מחזיר סיבה, השגרה מדפיסה אותה, והתחזית יוצאת כטקסט. שורת
ה-`RESULT` אומרת `chart=yes` או `chart=no`.

**מגבלת הכיתוב היא 1024 תווים והאנגלית קרובה אליה** (1015 ב-16/9). אם
הטקסט לא נכנס — התמונה יוצאת ראשונה עם כיתוב קצר וההודעה המלאה אחריה.
**זו לא הודעה כפולה; תחזית שנקטעת באמצע משפט גרועה יותר.**

**שני דומיינים חייבים להיות בהיתרי הרשת של סביבת `Bloowatch`:**
`www.surfline.com` ו-`wa.cdn-surfline.com`. בלי השני הדף עולה בלי עיצוב.
כל השאר שהדף פונה אליו — אנליטיקה, פרסומות, מפה, מצלמות — **נשאר חסום
ואינו נחוץ.** אם אחד מהם ייפול, הפרוקסי יחזיר 403 על CONNECT: **זו
מדיניות, לא תקלה. דווח, אל תעקוף.**

**למה זו פקודה אחת.** קודם `surfline.py` הדפיס שורת ארגומנטים, ומי שהריץ
את השגרה העתיק אותה ל-`forecast_message.py`. כל העברה כזאת היא מקום שבו
קריאה אחת יכולה להיות שונה מהקודמת, והשגרות נקראות כל ערב על ידי מודל
חדש. **שתיים משלוש התקלות של החודש לא היו באג בשום סקריפט — הן היו שיקול
דעת במקום שלא היה צריך אותו.** מה שנשאר לשיקול דעת הוא מה עושים כשזה
נכשל, וזה כתוב כאן.

`test_evening.py` בונה את אותה תחזית פעמיים — דרך הסקריפט ודרך שורת
הפקודה הישנה — ודורש שהן זהות. **אחרת השגרה מפסיקה לסטות והנוסח מתחיל**,
וזה גרוע יותר: אף אחד לא קורא שוב את הטקסט אחרי שהפקודה נראית מסודרת.

**אותם מספרים בדיוק בשתי השפות** — שתי הקבוצות קוראות אותו ים.

**הכללים של הבעלים, כולם כבר בקוד — אל תעקוף אותם ידנית:**

- **גובה:** הקריאה הנמוכה ביותר של אותו יום עד הגבוהה ביותר. לא ממוצע.
- **פריוד:** של **הסוואל הגדול ביותר**, לא הארוך.
- **רוח:** שורת הרוח היא של הבוקר; בלוק אונשור של שעתיים ומעלה מוסיף משפט.
- **שעות מומלצות:** מיד-טייד ±2:15, מעוגל למעלה לחצי שעה, חתוך ל-07:00–17:00.
- **ים חלש:** 90 דקות מכל גאות; המנוסים מקבלים חלון אחד דרך השפל,
  המתחילים מפוצלים סביבו. מעל הסף — כמו קודם, ושני הבלוקים זהים. הכלל
  נגזר מהתיקון שהבעלים כתב ביד ל-15/9 ומשחזר אותו לדקה.

  **האנרגיה נקראת מ-Surfline עצמו** — השדה `nearshore` בפיד `energy`,
  אותו מספר שמופיע בדף תחת NEARSHORE ENERGY, ביחידות kJ.

  **הסף נקבע ב-15/9/2026 על ידי הבעלים: "עד 250 זה יחסית חלש".**
  `WEAK_ENERGY = 250`, ו-250 עצמו נחשב חלש (`<=`, כי "עד 250" כולל אותו).

  לפני כן הוא עמד על 150, וזה היה ניחוש שלי — הנחתי אותו בדיוק מעל היום
  היחיד שהבעלים קרא לו חלש (**15/9 — 131 kJ**), והתייחסתי ל-**16/9 — 209**
  ול-**17/9 — 210** כימים רגילים. הם לא. הקו שלו גבוה בחצי מהניחוש שלי,
  והמשמעות היא שים חלש הוא **המקרה השכיח כאן ולא החריג** — וזה מה שים של
  0.6–1.1 מטר בחוף הזה באמת הוא. גם **18/9 — 132** ו-**19/9 — 112** חלשים.

  לפני זה זה היה פרוקסי (סכום של h²·T) והתיעוד טען שהסקאלה תואמת. היא לא:
  ב-16/9 הפרוקסי אמר 108 והדף של Surfline אמר 220, וזה התגלה רק כשהבעלים
  שלח צילום מסך מהטלפון שלו. **סף בלי יחידות הוא חסר משמעות.**
- **גשם:** שורה אחת, בלי שעות, רק כשיש גשם — "הגשם מוריד את הרוח ועושה
  את הים טוב לגלישה". ממטר וסופת רעמים מקבלים אותו משפט.

**שתי השגיאות שמסיימות את הריצה בלי לשלוח כלום**, ושתיהן מכוונות:

- `no Surfline hours ... do not guess` — הים לא נענה.
- `wind direction disagrees ...` — כיוון הרוח שלנו לא מסכים עם התווית של
  Surfline עצמו, וזה הופך כל משפט על הרוח.

**במקרים האלה: דווח ועצור. אל תריץ את הסקריפטים ביד, אל תיפול
ל-surf-forecast, ואל תמציא מספרים.** תחזית מומצאת שנשלחת למאתיים לקוחות
גרועה בהרבה מתחזית שלא נשלחה. **גיבוי מ-surf-forecast הוא החלטה של
הבעלים, לא של השגרה** — המספרים שם נמוכים בכשליש מטר, וזה שינוי בתוכן ולא
בדרך השליחה.

**אין צורך ב-`--dry-run` בשגרה.** הקבוצות נקבעות בקוד ו-`test_evening.py`
מוודא שעברית הולכת ל-`surfers_he` ואנגלית ל-`surfers_en`. **כל קבוצה
מקבלת פעם אחת בלבד** — קבוצה שקיבלה את התחזית פעמיים היא קבוצה שאנשים
עוזבים, ושליחה כפולה אי אפשר למשוך בחזרה.

אם `tides.py --check` מחזיר פחות מ-14 ימים, רענן מ-surf-forecast
(`--parse` ואז `--merge`) ובצע commit ל-`app/catalog.json`. **אם הרענון
נכשל — המשך עם מה שיש** וציין בדיווח; תחזית עם גאות של אתמול עדיפה על
תחזית שלא נשלחה.

## 3. לו״ז מחר לקבוצת הצוות — 19:00

```
python3 evening.py rota
```

**פקודה אחת, מ-14/9/2026.** קודם זה היו חמש, והשמירה של נקודת הייחוס הייתה
הפקודה האחרונה — כלומר ריצה שנעצרה באמצע יכלה לשלוח בלי לשמור, או לשמור
בלי לשלוח. שתיהן שוברות את בדיקת 20:00. עכשיו זה מעשה אחד: **נקודת הייחוס
נכתבת רק אחרי שהקבוצה באמת קיבלה**, ואף פעם לא לפני.

**קוד היציאה:** `0` = הלו״ז יצא ונקודת הייחוס נשמרה. `1` = משהו מזה לא
קרה — **דווח את שורת `RESULT` כמו שהיא.** היא אומרת אם הלוח היה צילום או
ציור, ואם נקודת הייחוס נשמרה.

**אין גיף.** הודעה אחת בערב, טקסט וצילום יחד — הטקסט הוא הכיתוב.

**הצילום:** `shot.py` מוצא לבד את הדפדפן שכבר על המכונה. אל תתקין playwright
ואל תוריד דפדפן. צפוי כ-220KB ורוחב 4286. **אל תשנה את הקובץ: בלי resize,
בלי quantize, בלי חיתוך.** הוא נראה מטושטש בתצוגה המקדימה וחד כשפותחים,
והבעלים ראה ואישר. הקטנה היא מה שהרס אותו בעבר.

- **הצילום נכשל** → `python3 board.py --date <מחר> --crew /tmp/crew.json --out /tmp/real.png`
  מצייר לוח במקום. לו״ז עם לוח מצויר עדיף על לו״ז בלי לוח. ציין בדיווח.
- **התמונה נכשלה בשליחה** → שלח לפחות את הטקסט, בלי `--file`.
- **כיתוב מעל 1024 תווים** → תמונה עם כיתוב קצר, והלו״ז המלא בהודעה נפרדת.
- **אין שיעורים מחר** → `rota.py` יחזיר "אין עדיין שיעורים". **אל תשלח
  לקבוצה**; שלח לבעלים אישית. יום ריק בקבוצה נראה כמו תקלה.
- **`rota.py` נכשל** → עצור, אל תשלח כלום, דווח.

**נקודת הייחוס היא חובה, ורק אחרי שליחה מוצלחת.** בלעדיה בדיקת השינויים של
20:00 עיוורת: היא תמצא תמונה של יום אחר ולא תשלח כלום. **ב-3/9/2026 נכנסו
ארבע הזמנות בין 19:00 ל-20:00, שתיים מהן לדילן שקיבל ב-19:15 "חופש נעים".**
**אם השליחה נכשלה — אל תשמור נקודת ייחוס:** תמונה של לו״ז שלא יצא גורמת
לבדיקה להשוות מול משהו שאיש לא ראה.

## 4. לו״ז אישי לכל מדריך — 19:15

```
python3 evening.py personal
```

**פקודה אחת, מ-14/9/2026.** ההבחנה שהפילה את זה בעבר — `--crew-out` כותב
את הקובץ ו-`--crew` קורא אותו, והחלפה ביניהם נכשלת בשקט — נמצאת עכשיו בקוד
ולא בקריאה של מי שמריץ.

**קוד היציאה:** `0` = כל מי שהיה אמור לקבל קיבל (או שלא היה אף אחד).
`1` = לא. `RESULT planned=N` אומר כמה היו בתוכנית. `/tmp/plan.json`
ו-`/tmp/crew.json` נמחקים לבד, גם כשהשליחה נכשלה.

`plan()` מחיל בעצמו את כל הכללים מ-`whatsapp.json`, והם בקוד ולא כאן כי
הסחיפה בהם היא לו״ז של מדריך אחד שנוחת בטלפון של אחר:

- **יובל מדולג** — זה המספר ששולח.
- **מי שב-`no_personal_message` מדולג.** זו החלטה של הבעלים — **אל תדווח
  עליהם כחסרי מספר.**
- **ויקטור וולאדי מקבלים אנגלית**, השאר עברית.
- **מי שאין לו מספר בבלו מדולג** ומופיע בדיווח. **אל תנחש מספר לעולם.**
- **מי שאין לו שיעורים ואינו בחופש לא מקבל כלום.** "אין לך שיעורים" כל ערב
  מפסיק להיקרא תוך שבוע. חופש זה ברכה, לא דיווח.
- **מי שמסומן בחופש אבל בכל זאת מלמד מחר מקבל לו״ז רגיל, לא ברכה.**

**`--crew` נכשל** → המשך בלי ברכות חופש וציין בדיווח. **`rota.py` נכשל** →
עצור, אל תשלח כלום, דווח.

דווח: כמה מלמדים מחר ולכמה נשלח, מי בחופש וקיבל ברכה, ומה נכשל.

## 5. בדיקת שינויים בלו״ז — 20:00

```
python3 evening.py changes
```

פקודה אחת. היא משווה את הלוח למה שיצא ב-19:00, ואם משהו זז — שולחת את
**מה שהשתנה בלבד** לקבוצה ולכל מדריך שזה נוגע אליו. אין מה להעתיק ואין
מה להחליט.

מה שורת ה-`RESULT` אומרת:

- `changed=0` → שום דבר לא זז מאז שבע. זה המצב ברוב הלילות.
- `changed=yes group=sent people=N/sent` → הקבוצה ו-N מדריכים עודכנו.
- `snapshot=missing` (יציאה 1) → **אין נקודת ייחוס.** או שהלו״ז של 19:00
  לא יצא, או שהוא לא נשמר. **אל תשלח את כל הלו״ז מחדש** — הודעה כזאת
  בשמונה בערב נקראת כתקלה. בדוק אם הלו״ז של 19:00 יצא, ודווח בשורה אחת.

**למה זה היה שבור, ואיך זה עובד עכשיו.** עד 15/9 הריצה של 19:00 שמרה את
הלוח לקובץ ב-`~/.shokogi/rota.json` והריצה של 20:00 קראה אותו. ברגע
שהשגרות עברו לקונטיינרים חדשים אלה הפכו לשתי מכונות שונות: הראשונה כתבה
את הקובץ ונזרקה, השנייה עלתה ריקה, לא מצאה כלום, הדפיסה "אין תמונה
להשוות מולה" — **ודיווחה על ריצה נקייה.** היא נכשלה בכיוון הבטוח, ולכן
אף אחד לא שם לב: כל לילה היא אמרה שהכל בסדר, וכל לילה היא לא ראתה כלום.

ה-repo לא יכול להחזיק את הקובץ — שגרה שנורית יכולה לשכפל אותו אבל לא
לדחוף אליו. אז התמונה נשמרת במקום שלשתי המכונות כבר יש אליו גישה ושתיהן
כבר קוראות ממנו: **יומן WhatsApp עצמו**, כהודעה מהמספר של בית הספר
לעצמו. הקוד ב-`snapshot.py`. שורת סימון בראש ההודעה מוודאת שהיא לא
מתבלבלת עם הודעה שנשלחה לאדם, ושרשתות הביטחון לא סופרות אותה כלו״ז
שיצא.

**הלקח, שוב:** ראיה שמשהו אחר יכול היה לייצר אינה ראיה. בדיקה שמדווחת
"נקי" כשהיא לא מסוגלת לראות כלום היא גרועה יותר מבדיקה שלא קיימת.

## 6. בדיקת מוכנות — 04:30, ו-7. רשת ביטחון — 18:25 ו-19:50

שלוש השגרות האלה לא שולחות שום דבר בימים רגילים. הן קיימות כי **שגרה לא
יכולה לדווח על הכישלון של עצמה**: הכשלים שחשובים הם בדיוק אלה שמונעים
ממנה לדווח. ריצה שנתקעת בהמתנה לאישור לא מסתיימת, ולכן גם לא מפעילה
התראת סיום — וזה מה שקרה ב-13/9.

**6. `preflight.py`, 04:30 — לפני שהיום מתחיל.**

```
python3 preflight.py
```

בודקת חמישה דברים שכל שגרה מניחה: שכל שישה הסודות נפתרים, ש-Green-API
מחזיר `authorized`, שההתחברות לבלו עובדת, כמה ימי גאות נשארו, **ושהקונטיינר
הזה בכלל מסוגל להריץ את הערב.** את אותן בדיקות כל שגרה עושה בעצמה —
**ההבדל הוא מתי מגלים.** שגרה מגלה ברגע שההודעה הייתה אמורה לצאת; זו מגלה
ארבעים דקות לפני משבצת התזכורת הראשונה, עם יום שלם לתקן.

**הבדיקה החמישית נוספה ב-15/9/2026, אחרי שהקובץ הזה קרא לבוקר תקין בזמן
שהערב היה שבור.** התחזית מתה על playwright חסר, וכל ארבע הבדיקות הקודמות
דיווחו ירוק — כי אף אחת מהן לא נוגעת בדפדפן. `deps` בודק את שתי התלויות
ואת הימצאות כרומיום, וחוסר הוא **`FAIL` ולא `WARN`**: בלעדיהן התחזית
והלו״ז לא יוצאים בכלל, והם מתים באמצע במקום לפני השליחה. ההודעה אומרת מה
חסר, וההתקנה בשלב 0 של השגרות היא התיקון.

הוא **לא מפעיל את הדפדפן**. להעלות כרומיום בארבע וחצי בבוקר רק כדי להוכיח
שהוא קיים היה איטי יותר ונכשל מסיבות שאין להן קשר לבוקר. מה שנעלם היה
ה-import והבינארי, אז זה מה שנבדק.

היא גם מדווחת **מאיפה כל סוד הגיע** — `environment` או `.env file`. זו
השורה שאומרת אם אפשר כבר למחוק את `printf` מהשגרות, **ולכן השגרה הזאת
היא היחידה שלא כותבת `.env` בעצמה**: קובץ שנכתב לפני הבדיקה הופך את
התשובה לחסרת ערך. ב-14/9 המחיקה נעשתה על סמך שורה אחת שאמרה
`environment` **בלי שריצת שגרה אמיתית אמרה את זה**, ושש תזכורות אבדו.
הפעם: שורת מקורות מריצה אמיתית, או שה-`printf` נשאר.

**7. `watch_sends.py`, 18:25 ו-19:50 — אחרי.**

**ב-14/9/2026 הבדיקה הזאת שיקרה, ותוקנה.** התחזית של 18:00 לא יצאה, והיא
אמרה `everything due by now went out`. הסיבה: היא חיפשה את פתיח התחזית
בחלון של 24 שעות — **והפתיח זהה בכל ערב**, אז התחזית של אתמול ענתה על
השאלה של היום. הבדיקה לא יכלה לתפוס תחזית חסרה, אף פעם.

**מאז, כל שאלה על "האם זה יצא" נושאת גם "מאיזה רגע".** `by_chat` מקבל
`since`, `watch_sends` מושך את היומן פעם אחת ומסנן לכל פריט מהדדליין שלו,
וביקורת התזכורות מוגבלת מחצות ולא נכנסת לערב של אתמול. נקודת החיתוך היא
כמה דקות **לפני** הדדליין, כדי ששגרה שהתחילה מוקדם עדיין תיספר.

```
python3 watch_sends.py
```

שואלת את **יומן ההודעות היוצאות של Green-API** — לא לוג מקומי, שיכול היה
להיכשל יחד עם השולח — מה מתוך שליחות הערב באמת הגיע: התחזית לשתי
הקבוצות, הלו״ז לקבוצת הצוות, והלו״זים האישיים. מה שחסר, השגרה **שולחת**,
לפי הפרק המתאים כאן, ואז מריצה את הבדיקה שוב כדי לסגור את הלולאה.

**הסקריפט רק מדווח; השליחה היא של השגרה.** כלי שגם מזהה וגם שולח הוא באג
אחד מלשלוח פעמיים.

**ההוכחה שהבדיקה יכולה להיכשל.** בריצה הראשונה מול נתונים אמיתיים היא
אמרה שהלו״ז של 19:00 לא יצא — והוא יצא. הלו״ז הוא `imageMessage` שהטקסט
שלו יושב ב-`caption`, וקורא היומן הכיר רק `textMessage`. **התראת שווא כל
לילה גרועה מאין בדיקה בכלל**, כי עד סוף השבוע זה מה שמדלגים עליו.

## 8. סגירת יום — 19:30

**כבויה מ-11/9/2026 לבקשת הבעלים.** הנוהל המלא נשאר ב-`CLOSING.md`
להחזרה מתי שיבקש. שים לב שהכיבוי עצר גם את עדכון הגיליון בדרייב, לא רק
את המייל — שניהם היו באותה שגרה.

---

## מה אף פעם לא

- **אל תשלח פעמיים "ליתר ביטחון".** הודעה כפולה גרועה מהודעה חסרה: את
  הראשונה אפשר להשלים, את השנייה אי אפשר למחוק.
- **אל תעקוף חסימה של הפרוקסי.** `403` שגופו `CONNECT tunnel failed` הוא
  מדיניות רשת. דווח.
- **אל תמציא מספר, אסימון, מחיר או תחזית.** אף פעם, בשום שלב.
- **אל תבצע commit לקבצים עם מספרי טלפון או שמות לקוחות.** ה-repo ציבורי,
  ו-`.github/no_pii.py` מפיל כל פרסום שמכיל אותם.
- **אל תסיק ששגרה עובדת מזה שהיא לא התריעה.** שגרה שנכשלת לפני שהיא נוגעת
  בערוץ שרשתות הביטחון מנטרות נכשלת **בשקט מוחלט** — זה מה שהסתיר חודש
  של תחזיות ולו״זים שלא יצאו אוטומטית. הבדיקה היחידה ששווה משהו היא
  `report.py --read`: מי באמת שלח, `auto` או `hand`.
